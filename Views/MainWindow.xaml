<Window x:Class="LessonScheduler.Views.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:local="clr-namespace:LessonScheduler.Views.UserControls"
        Title="🎓 Akıllı Ders Dağıtım Programı" 
        Height="800" Width="1200"
        MinHeight="600" MinWidth="800"
        WindowStartupLocation="CenterScreen"
        Icon="pack://application:,,,/Resources/school.ico"
        Style="{DynamicResource ModernWindow}">
    <DockPanel>
        <!-- Menu Bar -->
        <Menu DockPanel.Dock="Top" Style="{DynamicResource {x:Type Menu}}">
            <MenuItem Header="_Dosya">
                <MenuItem Header="JSON _Yükle" Command="{Binding LoadJsonCommand}"/>
                <MenuItem Header="JSON _Kaydet" Command="{Binding SaveJsonCommand}"/>
                <Separator/>
                <MenuItem Header="_Test Verisini Yükle" Command="{Binding LoadTestDataCommand}"/>
            </MenuItem>
            <MenuItem Header="_İşlemler">
                <MenuItem Header="_Dağıtımı Başlat" Command="{Binding RunSolverCommand}"/>
            </MenuItem>
            <MenuItem Header="_Ayarlar">
                <MenuItem Header="⚙️ Okul Ayarları" Click="OpenSettingsTab"/>
            </MenuItem>
            <MenuItem Header="_PDF Export">
                <MenuItem Header="📄 Sınıf Programları (Toplu)" Command="{Binding ExportClassPdfCommand}"/>
                <MenuItem Header="📄 Sınıf Programları (Ayrı Dosyalar)" Command="{Binding ExportClassPdfSeparateCommand}"/>
                <Separator/>
                <MenuItem Header="👨‍🏫 Öğretmen Programları (Toplu)" Command="{Binding ExportTeacherPdfCommand}"/>
                <MenuItem Header="👨‍🏫 Öğretmen Programları (Ayrı Dosyalar)" Command="{Binding ExportTeacherPdfSeparateCommand}"/>
            </MenuItem>
        </Menu>

        <!-- Status Bar -->
        <StatusBar DockPanel.Dock="Bottom">
            <TextBlock Text="{Binding StatusMessage}"/>
        </StatusBar>

        <!-- Main Content -->
        <TabControl x:Name="MainTabControl" Background="White" Foreground="Black">
            <TabItem Header="⚙️ Ayarlar" Background="#F5F5F5" Foreground="Black">
                <Border Background="White" CornerRadius="8" Margin="10" 
                        BorderBrush="#E0E0E0" BorderThickness="1">
                    <Border.Effect>
                        <DropShadowEffect Color="Gray" Direction="315" ShadowDepth="2" Opacity="0.3"/>
                    </Border.Effect>
                    <ScrollViewer VerticalScrollBarVisibility="Auto">
                        <StackPanel Margin="20">
                            <!-- Header -->
                            <TextBlock Text="⚙️ Okul Ayarları" FontSize="24" FontWeight="Bold" 
                                       Foreground="DarkBlue" Margin="0,0,0,20"/>
                            
                            <!-- School Information Section -->
                            <GroupBox Header="🏫 Okul Bilgileri" Margin="0,0,0,20">
                                <Grid Margin="10">
                                    <Grid.RowDefinitions>
                                        <RowDefinition Height="Auto"/>
                                        <RowDefinition Height="Auto"/>
                                        <RowDefinition Height="Auto"/>
                                    </Grid.RowDefinitions>
                                    <Grid.ColumnDefinitions>
                                        <ColumnDefinition Width="150"/>
                                        <ColumnDefinition Width="*"/>
                                    </Grid.ColumnDefinitions>
                                    
                                    <!-- School Name -->
                                    <Label Grid.Row="0" Grid.Column="0" Content="Okul Adı:" 
                                           VerticalAlignment="Center" FontWeight="SemiBold"/>
                                    <TextBox Grid.Row="0" Grid.Column="1" 
                                             Text="{Binding SettingsViewModel.SchoolName, UpdateSourceTrigger=PropertyChanged}"
                                             Margin="5" Padding="8" MaxLength="100"/>
                                    
                                    <!-- Principal Name -->
                                    <Label Grid.Row="1" Grid.Column="0" Content="Müdür Adı:" 
                                           VerticalAlignment="Center" FontWeight="SemiBold"/>
                                    <TextBox Grid.Row="1" Grid.Column="1" 
                                             Text="{Binding SettingsViewModel.PrincipalName, UpdateSourceTrigger=PropertyChanged}"
                                             Margin="5" Padding="8" MaxLength="100"/>
                                    
                                    <!-- Info Text -->
                                    <TextBlock Grid.Row="2" Grid.Column="0" Grid.ColumnSpan="2" 
                                               Text="💡 Bu bilgiler PDF çıktılarında görüntülenecektir."
                                               Foreground="Gray" FontStyle="Italic" Margin="0,10,0,0" TextWrapping="Wrap"/>
                                </Grid>
                            </GroupBox>
                            
                            <!-- Daily Hours Section -->
                            <GroupBox Header="📅 Günlük Ders Saati Ayarları" Margin="0,0,0,20">
                                <StackPanel Margin="10">
                                    <!-- Info Text -->
                                    <TextBlock Text="💡 Her gün için farklı ders saati sayısı belirleyebilirsiniz."
                                               Foreground="Gray" FontStyle="Italic" Margin="0,0,0,15" TextWrapping="Wrap"/>
                                    
                                    <!-- Daily Hours Grid -->
                                    <ItemsControl ItemsSource="{Binding SettingsViewModel.DailyHours}">
                                        <ItemsControl.ItemTemplate>
                                            <DataTemplate>
                                                <Border Background="LightBlue" BorderBrush="Gray" 
                                                        BorderThickness="1" CornerRadius="5" 
                                                        Margin="0,2" Padding="10">
                                                    <Grid>
                                                        <Grid.ColumnDefinitions>
                                                            <ColumnDefinition Width="120"/>
                                                            <ColumnDefinition Width="*"/>
                                                            <ColumnDefinition Width="100"/>
                                                        </Grid.ColumnDefinitions>
                                                        
                                                        <!-- Day Name -->
                                                        <TextBlock Grid.Column="0" Text="{Binding DayName}" 
                                                                   VerticalAlignment="Center" FontWeight="SemiBold"/>
                                                        
                                                        <!-- Hours Slider -->
                                                        <Slider Grid.Column="1" 
                                                                Value="{Binding Hours, UpdateSourceTrigger=PropertyChanged}"
                                                                Minimum="{Binding MinHours}" Maximum="{Binding MaxHours}"
                                                                TickFrequency="1" IsSnapToTickEnabled="True"
                                                                VerticalAlignment="Center" Margin="10,0"/>
                                                        
                                                        <!-- Hours Display -->
                                                        <StackPanel Grid.Column="2" Orientation="Horizontal" 
                                                                    VerticalAlignment="Center" HorizontalAlignment="Center">
                                                            <TextBlock Text="{Binding Hours}" FontWeight="Bold" 
                                                                       FontSize="16" Foreground="DarkBlue"/>
                                                            <TextBlock Text=" saat" Margin="2,0,0,0" Foreground="Gray"/>
                                                        </StackPanel>
                                                    </Grid>
                                                </Border>
                                            </DataTemplate>
                                        </ItemsControl.ItemTemplate>
                                    </ItemsControl>
                                </StackPanel>
                            </GroupBox>
                            
                            <!-- Action Buttons -->
                            <StackPanel Orientation="Horizontal" HorizontalAlignment="Right" Margin="0,20,0,0">
                                <!-- Unsaved Changes Indicator -->
                                <TextBlock Text="⚠️ Kaydedilmemiş değişiklikler var" 
                                           Foreground="Orange" FontWeight="SemiBold" 
                                           VerticalAlignment="Center" Margin="0,0,20,0"/>
                                
                                <!-- Reset Button -->
                                <Button Content="🔄 Varsayılana Sıfırla" Command="{Binding SettingsViewModel.ResetToDefaultCommand}"
                                        Margin="0,0,10,0" Padding="15,8" MinWidth="120" Background="LightGray"/>
                                
                                <!-- Apply Button -->
                                <Button Content="✅ Uygula" Command="{Binding SettingsViewModel.ApplyChangesCommand}"
                                        Margin="0,0,10,0" Padding="15,8" MinWidth="100" Background="LightGreen"/>
                                
                                <!-- Save Button -->
                                <Button Content="💾 Kaydet" Command="{Binding SettingsViewModel.SaveSettingsCommand}"
                                        Padding="15,8" MinWidth="100" Background="LightBlue"/>
                            </StackPanel>
                        </StackPanel>
                    </ScrollViewer>
                </Border>
            </TabItem>
            <TabItem Header="📚 1. Tanımlamalar" Background="#F5F5F5" Foreground="Black">
                <Border Background="White" CornerRadius="8" Margin="10" 
                        BorderBrush="#E0E0E0" BorderThickness="1">
                    <Border.Effect>
                        <DropShadowEffect Color="Gray" Direction="315" ShadowDepth="2" Opacity="0.3"/>
                    </Border.Effect>
                    <local:DefinitionsControl DataContext="{Binding DefinitionsViewModel}"/>
                </Border>
            </TabItem>
            <TabItem Header="📝 2. Ders Atamaları" Background="#F5F5F5" Foreground="Black">
                <Border Background="White" CornerRadius="8" Margin="10" 
                        BorderBrush="#E0E0E0" BorderThickness="1">
                    <Border.Effect>
                        <DropShadowEffect Color="Gray" Direction="315" ShadowDepth="2" Opacity="0.3"/>
                    </Border.Effect>
                    <local:AssignmentsControl DataContext="{Binding AssignmentsViewModel}"/>
                </Border>
            </TabItem>
            <TabItem Header="⏰ 3. Kısıtlamalar" Background="#F5F5F5" Foreground="Black">
                <Border Background="White" CornerRadius="8" Margin="10" 
                        BorderBrush="#E0E0E0" BorderThickness="1">
                    <Border.Effect>
                        <DropShadowEffect Color="Gray" Direction="315" ShadowDepth="2" Opacity="0.3"/>
                    </Border.Effect>
                    <local:ConstraintsControl DataContext="{Binding ConstraintsViewModel}"/>
                </Border>
            </TabItem>
            <TabItem Header="🏫 Sonuç: Sınıf Programları" Background="#F5F5F5" Foreground="Black">
                <Border Background="White" CornerRadius="8" Margin="10" 
                        BorderBrush="#E0E0E0" BorderThickness="1">
                    <Border.Effect>
                        <DropShadowEffect Color="Gray" Direction="315" ShadowDepth="2" Opacity="0.3"/>
                    </Border.Effect>
                    <local:ResultsControl DataContext="{Binding ClassResultsViewModel}"/>
                </Border>
            </TabItem>
            <TabItem Header="👨‍🏫 Sonuç: Öğretmen Programları" Background="#F5F5F5" Foreground="Black">
                <Border Background="White" CornerRadius="8" Margin="10" 
                        BorderBrush="#E0E0E0" BorderThickness="1">
                    <Border.Effect>
                        <DropShadowEffect Color="Gray" Direction="315" ShadowDepth="2" Opacity="0.3"/>
                    </Border.Effect>
                    <local:ResultsControl DataContext="{Binding TeacherResultsViewModel}"/>
                </Border>
            </TabItem>
            <TabItem Header="📊 Sonuç: Kalite Raporu" Background="#F5F5F5" Foreground="Black">
                <Border Background="White" CornerRadius="8" Margin="10" 
                        BorderBrush="#E0E0E0" BorderThickness="1">
                    <Border.Effect>
                        <DropShadowEffect Color="Gray" Direction="315" ShadowDepth="2" Opacity="0.3"/>
                    </Border.Effect>
                    <TextBox Text="{Binding QualityReport}" IsReadOnly="True" 
                            VerticalScrollBarVisibility="Auto" FontFamily="Consolas" 
                            Background="{DynamicResource CardBrush}"
                            Foreground="{DynamicResource TextPrimaryBrush}"
                            BorderBrush="{DynamicResource BorderBrush}"/>
                </Border>
            </TabItem>
            <TabItem Header="📋 Sonuç: Çözüm Özeti"
                     Background="{DynamicResource CardBrush}"
                     Foreground="{DynamicResource TextPrimaryBrush}">
                <Border Style="{DynamicResource Card}">
                    <TextBox Text="{Binding SolutionSummary}" IsReadOnly="True" 
                            FontFamily="Consolas" VerticalScrollBarVisibility="Auto"
                            Background="{DynamicResource CardBrush}"
                            Foreground="{DynamicResource TextPrimaryBrush}"
                            BorderBrush="{DynamicResource BorderBrush}"/>
                </Border>
            </TabItem>
        </TabControl>
        

    </DockPanel>
</Window>